<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>UnHax</title>
  <style>
    body {
      margin: 0;
      font-family: 'Courier New', monospace;
      background-color: #0d0d0d;
      color: #33ff33;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      height: 100vh;
      overflow: hidden;
    }

    h1 {
      font-size: 3rem;
      animation: glitch 1s infinite;
    }

    p {
      font-size: 1.2rem;
      margin-top: 1rem;
    }

    @keyframes glitch {
      0% { text-shadow: 2px 2px #ff0000; }
      25% { text-shadow: -2px -2px #00ffff; }
      50% { text-shadow: 2px -2px #ff00ff; }
      75% { text-shadow: -2px 2px #00ff00; }
      100% { text-shadow: 2px 2px #ff0000; }
    }

    .btn {
      margin-top: 2rem;
      padding: 10px 20px;
      background-color: transparent;
      border: 2px solid #33ff33;
      color: #33ff33;
      font-size: 1rem;
      cursor: pointer;
      transition: background 0.3s;
    }

    .btn:hover {
      background-color: #33ff33;
      color: #000;
    }
  </style>
</head>
<body>
  <h1>UnHax</h1>
  <p>The future of open-source exploits, ethically done.</p>
  <button class="btn" onclick="alert('Coming soon!')">Enter</button>
</body>
</html>
